{{>header}}
import 'package:{{{pubName}}}/api.dart';
import 'package:test/test.dart';

/// tests for {{{clientName}}}
void main() {
  group('simple initialization test for {{{clientName}}}', () {
    test('test {{{clientName}}}()', () {
      final instance = {{{clientName}}}();
      expect(instance, isNotNull);
    });
  });
}
