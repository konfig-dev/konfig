{{#if hasVars}}
class {{classname}}(BaseModel):
    {{#each requiredVars}}
        {{#if description}}
    # {{{description}}}
        {{/if}}
    {{baseName}}: {{{dataType}}}
    {{#unless @last}}

    {{/unless}}
    {{/each}}
    {{#each optionalVars}}
        {{#if description}}
    # {{{description}}}
        {{/if}}
    {{baseName}}: {{{dataType}}} = Field(None)
    {{#unless @last}}

    {{/unless}}
    {{/each}}
    {{#unless requiredVars}}
    {{#unless optionalVars}}
    pass
    {{/unless}}
    {{/unless}}
{{/if}}
