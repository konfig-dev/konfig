name: Custom Action Workflow

on:
  push:
    branches:
      - main # Change this to the desired branch name

jobs:
  run-custom-action:
    runs-on: ubuntu-latest
    
    steps:

    - name: Start
      run: echo "Start"

    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Run Custom Action
      uses: konfig-dev/konfig/actions/merge-pull-request@main

    - name: End
      run: echo "End"